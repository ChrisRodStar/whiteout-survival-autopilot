name: Mail Read Alliance

node: mail_alliance

priority: 25

steps:
  - click: mail_read_and_claim_all
  - wait: 1s
  - click: mail_read_and_claim_all # second click to skip reward message (if any)
  - wait: 1s
  - action: screenshot # take screenshot to verify all mail has been read
    analyze:
      - name: mail.state.isAlliance
        action: text
        type: integer
        threshold: 0.9
